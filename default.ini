[providers]
  [[torrent]]
    [[[__many__]]]
      enabled = boolean(default=False)
      custom_url = string()
      api_key = string()
      username = string()
      password = string()
      passkey = string()
      pin = string()
      confirmed = boolean(default=True)
      ranked = boolean(default=True)
      engrelease = boolean(default=False)
      onlyspasearch = boolean(default=False)
      sorting = option(last, seeders, leechers, default=seeders)
      ratio = string()
      minseed = integer(0, default=0)
      minleech = integer(0, default=0)
      freeleech = boolean(default=False)
      search_mode = option(eponly, sponly, default=eponly)
      search_fallback = boolean(default=False))
      enable_daily = boolean(default=False)
      enable_backlog = boolean(default=False)
      cat = integer(0, default=0)
      subtitle = boolean(default=False)
      cookies = string()
    [[nzb]]
      [[[__many__]]]
        enabled = boolean(default=False)
        url = string()
        api_key = string()
        username = string()
        password = string()
        passkey = string()
        pin = string()
        confirmed = boolean(default=True)
        ranked = boolean(default=True)
        engrelease = boolean(default=False)
        onlyspasearch = boolean(default=False)
        sorting = option(last, seeders, leechers, default=seeders)
        options = string()
        ratio = string()
        minseed = integer(0, default=0)
        minleech = integer(0, default=0)
        freeleech = boolean(default=False)
        search_mode = option(eponly, sponly, default=eponly)
        search_fallback = boolean(default=False))
        enable_daily = boolean(default=False)
        enable_backlog = boolean(default=False)
        cat = integer(0, default=0)
        subtitle = boolean(default=False)
        cookies = string()
[clients]
  [[nzb]]
  [[torrent]]

[metadata]
  [[__many__]]
    show_metadata = boolean(default=False)
    episode_metadata = boolean(default=False)
    fanart = boolean(default=False)
    poster = boolean(default=False)
    banner = boolean(default=False)
    episode_thumbnails = boolean(default=False)
    season_posters = boolean(default=False)
    season_banners = boolean(default=False)
    season_all_poster = boolean(default=False)
    season_all_banner = boolean(default=False)
