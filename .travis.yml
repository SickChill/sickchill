language: python
python:
  - 2.6
  - 2.7

env:
  - DB=sqlite://

branches:
  only:
    - develop

install:
  - pip install cheetah

before_script:
  - cd ./tests
  - chmod +x ./all_tests.py
  
script:
  - ./all_tests.py

notifications:
  irc: "irc.freenode.net#sickrage"